# Choose ONE of these:
# Local Redis when running compose profile 'redis-local'
REDIS_URL=redis://redis:6379/0
# Or Upstash for prod-like tests (don’t run the local redis service)
# REDIS_URL=rediss://:<token>@<upstash-host>:<port>

RUST_LOG=info
